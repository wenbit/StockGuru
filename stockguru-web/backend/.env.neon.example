# Neon 数据库配置示例
# 复制此文件为 .env 并填入实际值

# ===========================================
# 数据库配置 (Neon PostgreSQL)
# ===========================================

# Neon 数据库连接字符串
# 格式: postgresql://[user]:[password]@[host]/[database]?sslmode=require
# 示例: postgresql://stockguru_owner:npg_xxx@ep-xxx-xxx.aws.neon.tech/stockguru?sslmode=require
DATABASE_URL=postgresql://your_user:your_password@your_host/your_database?sslmode=require
NEON_DATABASE_URL=postgresql://your_user:your_password@your_host/your_database?sslmode=require

# ===========================================
# Python 环境配置
# ===========================================

# Python 版本 (Render 使用)
PYTHON_VERSION=3.12.0

# 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ===========================================
# 应用配置
# ===========================================

# API 端口 (本地开发使用，生产环境由 Render 自动设置)
PORT=8000

# CORS 允许的源（生产环境）
# 多个源用逗号分隔
ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:3000

# ===========================================
# 注意事项
# ===========================================

# ⚠️ 不再需要的 Supabase 配置（已移除）：
# - SUPABASE_URL
# - SUPABASE_KEY
# - SUPABASE_DB_PASSWORD

# ✅ Neon 优势：
# - 更快的连接速度
# - 更好的性能
# - 自动扩展
# - 免费套餐：500MB 存储

# 📝 获取 Neon 连接字符串：
# 1. 访问 https://console.neon.tech/
# 2. 选择你的项目
# 3. 点击 "Connection Details"
# 4. 复制 "Connection string"
